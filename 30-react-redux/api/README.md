# Step by step

Add script in `package.json`:

```code
 "scripts": {
    "start": "node index.js",
  },
```

`npm init -y`
`npm i express cors`
`npm i nodemon`

Add new script in 'package.json'
`"dev": "nodemon index.js"`
